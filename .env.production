# 生产环境构建变量(仅构建时使用,不影响运行时)

# 启用 config.js 外置:把 src/config/index.js 抽成 dist 下一个独立明文 JS 文件
# 发版后可直接在服务器编辑该文件修改配置(后端地址/站点名等),无需重新构建
VUE_APP_CONFIGJS=true

# 站点标题(可选,留空则用 index.html / config 里的默认值)
VUE_APP_TITLE=EZ THEME

# 环境标识(可选)
VUE_APP_ENV=production
